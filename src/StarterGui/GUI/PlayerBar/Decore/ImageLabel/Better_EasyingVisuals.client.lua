local ReplicatedStorage = game:GetService("ReplicatedStorage")

local EasyVisuals = require(ReplicatedStorage.Utilities.ClientModuleUtils._EasyVisuals)

local image = script.Parent


EasyVisuals.new(image, "ZebraRed", 0.4, 0,false)
